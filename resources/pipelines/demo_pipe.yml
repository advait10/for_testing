resources:
  pipelines:
    pipeline_demo_pipeline:
      name: demo_pipeline
      libraries:
        - notebook:
            path: ../bu1/demo.sql
      schema: default
      continuous: false
      development: true
      photon: true
      channel: CURRENT
      catalog: workspace
      serverless: true
